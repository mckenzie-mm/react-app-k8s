apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: todo-mysql-data
  name: todo-mysql-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
